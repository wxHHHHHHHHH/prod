server:
  port: 8085

spring:
  application:
    name: payment-service
  datasource:
    url: ${MYSQL_URL:jdbc:mysql://47.108.130.167:3306/mall}?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER:47.108.130.167:8848}
        namespace: dev

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
